import { App, ButtonComponent, Modal, Notice, Plugin, PluginSettingTab, Setting, TextComponent } from 'obsidian';


export default class GitStore extends Plugin {
    async onload() {
		console.log('Loading git storex§.');
    }
}